---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: showroom-proxy-watcher
  namespace: rhai-workshop-plugin
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: showroom-proxy-watcher
rules:
- apiGroups: [""]
  resources: ["namespaces"]
  verbs: ["get", "list", "watch"]
- apiGroups: [""]
  resources: ["configmaps"]
  verbs: ["get", "list", "create", "update", "patch"]
- apiGroups: ["apps"]
  resources: ["deployments"]
  verbs: ["get", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: showroom-proxy-watcher
subjects:
- kind: ServiceAccount
  name: showroom-proxy-watcher
  namespace: rhai-workshop-plugin
roleRef:
  kind: ClusterRole
  name: showroom-proxy-watcher
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: showroom-proxy-watcher
    app.kubernetes.io/component: showroom-proxy-watcher
    app.kubernetes.io/part-of: rhai-workshop-plugin
  name: showroom-proxy-watcher
  namespace: rhai-workshop-plugin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: showroom-proxy-watcher
  template:
    metadata:
      labels:
        app: showroom-proxy-watcher
    spec:
      serviceAccountName: showroom-proxy-watcher
      containers:
      - name: showroom-proxy-watcher
        image: quay.io/eformat/showroom-proxy-watcher:latest
        imagePullPolicy: Always
        args:
        - --plugin-namespace=rhai-workshop-plugin
        - --plugin-deployment=rhai-workshop-plugin
        - --reconcile-frequency=30s
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
